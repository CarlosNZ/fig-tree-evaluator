# FigTree v3 — implementation plan

_Working document. The build sequence: discrete, individually-testable chunks in dependency order — review and amend freely; once agreed, each chunk likely becomes a GitHub issue and we work through them top to bottom. This plan sequences steps 2–3 of [v3-testing-strategy.md](v3-testing-strategy.md) (hand-migrate tests + implement, interleaved per chunk); its step 1 is Phase 0 here, and its steps 4–5 are Phase 15._

## Working rules (apply to every chunk)

1. **Tests first, per chunk.** Before building a chunk, write its tests: hand-migrated v2 tests where v2 coverage exists (the independent-oracle rule — never converter-generated), new tests for new semantics. The [gradient register](v3-cases-for-review.md) rows and the [worked examples](v3-worked-examples.md) are named test sources — each chunk lists which rows it discharges.
2. **A chunk is done when**: its tests pass; every previously-green suite still passes; and any divergence from the spec discovered while building has been taken **back to the spec docs for a ruling** before moving on (the operator-contract doc's standing rule — implementation reshapes details, but the docs stay the source of truth).
3. **Interfaces early, features behind them.** Cross-cutting interfaces (`OperatorContext` with `signal` / `cache.memo` / `trace.note`) exist as working stubs from the first evaluator chunk, so operator bodies are written in final shape once and features (caching, tracing, abort) light up behind stable interfaces later — no retrofit passes over 42 operator bodies.
4. **The frozen `V2/` corpus is never edited** and never runs against v3 source — it is a record and the converter's oracle (Phase 15), nothing else.
5. **No chunk starts until the previous one is validated whole** — the discrete-chunks requirement. Chunks within a phase are sequential unless marked parallel-safe.
6. **Every phase closes with a build and a size reading.** Run `pnpm build` (it must be green — CI runs it too) and add the phase's row to the [bundle-size table](#bundle-size-by-phase). Growth is watched as it happens, not audited at Phase 14.
7. **Every phase closes with a runnable showcase** (Carl, September 2026): `src/dev/phase<N>_showcase.ts`, run with `pnpm dev phase<N>_showcase` — a range of expressions exercising the phase's features, printing each expression and its result or error. Not a test (the suites are the assertions); a reading of the phase for humans, and the seed material for release notes. First instance: `phase4_showcase.ts`. TO-DO: retroactive showcases for Phases 1–3 (primitives and errors; definitions and registry; the compiler and `validate()`).

---

## Phase 0 — Scaffolding & corpus freeze

**Posture: full rewrite.** No v2 engine code is ported — the architecture inverts at every layer (per-visit recognition → compile-once; body-side checks → engine layers; alias machinery deleted), and v2's systemic metadata/runtime default drift makes copying actively hazardous. What does carry over, as assets rather than code: the **test corpus** (0.1); the **v2 source as data** for Phase 15's converter (the alias tables, property aliases and `parseChildren` positional mappings are the authoritative record of what v2 accepted — mined, never ported); **reference implementations** for fiddly corners (fetch/axios error-payload extraction, `dequal` semantics in EQUAL — the dependency itself likely survives); and the package/publish/CI plumbing, adjusted in 0.2 and Phase 14.

**0.1 · Freeze the v2 corpus.** Copy the current `test/` suite into `test/V2/`, immutable, expected outputs included (testing-strategy step 1). Tag which files are pure expression-tree tests vs infrastructure tests (options/cache/HTTP/SQL wiring) — only the former flow through the eventual differential. The v2 source is **renamed to `/v2-src` and kept in-repo until v3 approaches release** (Carl's call): the frozen corpus stays _runnable_ against it (on demand, not in v3 CI) — live v2 behaviour on tap during hand-migration and Phase-15 divergence cataloguing, not just recorded expectations.

**0.2 · v3 skeleton.** Fresh `src/` beside `/v2-src` on `v3.0-dev`; tsconfig/eslint/rollup exclude `/v2-src` entirely (plus an import-ban lint so v3 source can never reach into it); jest config for the v3 suite; CI running the v3 suite only. Test doubles built once, used everywhere: a **scripted mock `HttpClient`** (fixed responses, failure/latency switches, call counter) and a **recording `CacheStore`** (`{ get, set }` logging keys) — the observability harness the worked examples assume.

---

## Phase 1 — Foundations (pure functions, zero dependencies)

**1.1 · Shared primitives.** The one-function requirements from [v3-implementation-notes.md](v3-implementation-notes.md): `isTruthy`, the ordering comparator, the stringification renderer (`renderText` with `<array>`/`<object>` placeholders), the shared whitespace set / trim, code-point segmentation, decimal-representation rounding, and the **path resolver** (dot/bracket grammar, `[*]` projection, null drill-through, own-enumerable-only, `strictDataPaths` flag). Pure unit tests; these are also the exported author-facing helpers, so their tests are contract tests.
_Spec: implementation notes; Type area; References §3._

**1.2 · `FigTreeError` + `Issue`.** The error class (code / path / holePath / fragment+fragmentPath / errorData / related / cause / issues / trace slot / `prettyPrint`), the issue shape, and a first cut of the stable `code` vocabulary. `OperatorFailure` (contract Q6) decided here.
_Spec: evaluator-methods § FigTreeError; contract § Runtime interface._

**1.3 · Type vocabulary checker.** The metadata types (basics, `integer`, unions, literal unions) plus `constraints` (length / homogeneous / elementShape). One table, consumed later by three moments (registration, compile, runtime) — tested standalone against fixtures.
_Spec: Type area § Metadata type vocabulary; contract § Constraints._

---

## Phase 2 — Definitions & registry (still no evaluation)

**2.1 · `defineOperator()`.** Definition validation in isolation: name/alias legality + reservation set, parameter declarations, `positionalParams` rules, nullPolicy declarability (type-driven admission), **conditional nullPolicy compilation** (enumerate the literal union → policy table), `replacesNullAt` constraints, `timeoutParam` (contract Q5 resolved: definition-level pointer), `returns`, the `EvaluationData` sentinel. Fixture-based tests: every registration error in the contract's validation list gets a fixture that triggers it.
_Spec: contract §§ Definition shape → Registration & validation._

**2.2 · Registry assembly.** Building the instance registry from the `operators` array: flattening, cross-registry collision checks (one namespace), alias map, `operatorDefaults` validation against metadata — including the **required-parameter ban** (evaluator-methods Q12, signed off). `new FigTree()` exists as a shell that registers and throws on bad input; no `evaluate` yet. (`excludeOperators` removed from v3 — Options ruling, July 2026, at Phase-2 planning.)
_Spec: Options § Operator registration, § operatorDefaults; Operators §4–5._

---

## Phase 3 — The compiler (the artifact), with `validate()` as its public face

**3.1 · Artifact obligations checklist + internal types.** First act: consolidate the artifact's scattered obligations (hole paths, constancy, shielding flags + static-fallback constants, issue stream, depth/node counts, canonical normalization, delivery-mode binding, compiled regexes, option-independence, self-containment) into a checklist and design the internal artifact types against it. Internal and freely refactorable — the checklist, not the types, is the contract.

**3.2 · Compile / normalize / classify.** The single walk: node-kind recognition, shorthand + alias normalization, positional mapping via `positionalParams`, the sibling-key rule, all malformed-node hard errors, `//` stripping, `vars` block structure, `literal` boundaries, reference-token recognition, name legality, constancy classification bottom-up, skeleton + hole extraction, counts. Tests assert artifacts (white-box, internal) _and_ the issue stream.
_Spec: Node grammar, entire; Operators § Shorthand grammar; References §§1–3, 7._

**3.3 · Static validation + `validate()`.** The metadata-driven layer on the artifact: literal parameter type checks + constraints, missing-required / unknown-key, empty-literal-aggregate, unresolved `$vars`/`$params`/`$element`/`$index`, vars cycles, `as` collisions, `returns` feeding-position check, operator `validate` hooks, all warnings (unrecognized-`$`, shadowing, unreferenced vars, useless modifiers — the excluded-operator row is retired with `excludeOperators`), `timeoutShielded` computation. Public `fig.validate()` ships here — the compiler's test surface goes black-box from this point. _(Built July 2026 as a second compile-time pass over the compiled AST — ruled with Carl at Phase-3 planning: identical asymptotics to the literal single walk for the constant-heavy workloads, since pass 2 never descends into collapsed constants. The empty-literal-aggregate check has no generic trigger yet — whether each aggregate's check is hook-authored or earns a declarative constraint is decided at Phase 4's first aggregate.)_
_Spec: evaluator-methods § validate (the check inventory table is the test list); contract § validate hook._

---

## Phase 4 — Evaluator core: eager evaluation, throw mode

**4.0 · Carried from Phase 3 — the recursion ceiling (defect, found September 2026).** `maxDepth` is measured during the walk and compared afterwards, so a deep input throws `RangeError: Maximum call stack size exceeded` out of the walk before the check can report: with `maxDepth: 50` set, depth 1,000 reports `max-depth` but depth 1,500 throws — breaking `validate()`'s never-throws-on-content invariant, and defeating the one rationale that clearly justifies a depth limit (stack safety, which applies to inert data as much as to expressions). Add a built-in, option-independent ceiling inside `walk()` (conservative, ~500 — environments differ), emitting an error issue and refusing to descend; the user's `maxDepth` stays the per-call check against the measured depth. The Phase-4 constancy probe (implementation notes) recurses too and carries the same ceiling. Tests: a 5,000-deep input reports instead of throwing, through both `validate()` and `evaluate()`.
Same chunk, same root cause: **`nodeCount` narrows to count _evaluable_ nodes only** (ruled September 2026, Carl — _provisional, up for further refinement_), so inert data stops tripping a limit meant for runaway logic, and a built-in mid-walk ceiling bounds walk cost on untrusted input instead. Tests: a 200-entry options list no longer trips `maxNodes: 500`; an expression with 600 operators does.
_Built (September 2026): evaluable = operator, fragment-call, reference and invalid nodes — constants and plain containers are structure, not work (Carl's ruling at Phase-4 planning). The ceiling is `DEPTH_CEILING = 500` in `src/compile/probe.ts`, reported as code `depth-ceiling`; the probe is the shared `probeConstant`, its shipping property `probe(x).constant === (compile(x).root is a constant holding x itself)` — stricter than the notes' `holes.length === 0`, since a `//` key, a `vars` block or an `undefined` value normalizes the value without adding a hole. Also carried here: the static layer type-checked literal nulls before applying null policy, so register rows 13 and 14 failed `validate()` — fixed to mirror the runtime order (unset at optionals, element policy before constraints)._
_Spec: obligations B4 (amended); implementation notes § `maxDepth` cannot currently do its job; evaluator-methods § validate() — the process._

**4.1 · The dispatch + engine layers.** The four-kind recursive evaluation (constant / reference / node / skeleton + splice), eager parameter resolution, runtime type checks, engine-side null-policy enforcement (propagate short-circuit, type-driven admission, null-means-unset, `replacesNullAt`), boundary normalization (`undefined`→`null`, finite guard, escaped-handle guard), **fallback rules 1–2 and 4–6**, `evaluate()` with minimal per-call merge, `mode: 'throw'`. `OperatorContext` lands with working `signal` passthrough and **stub** `cache.memo` (identity) and `trace.note` (no-op) — rule 3 above.
_Built (September 2026): `operatorDefaults` **parameter defaults and the default-`fallback` catch land here, not in 8.1** — the unset chain needs the defaults, and the compiler already counts a default fallback toward shielding, so the runtime catch must honour it; the `replacesNullAt` holder is an internal evaluate-once thunk (`once()` in src/utils.ts) that Phase 5 wraps into the body-facing `LazyValue`; the two-level option merge is implemented as `mergeOptions` (src/evaluate/context.ts), leaving 8.1 `updateOptions`/`getOptions`/re-validation; the probe fast path runs before the compile (off when `trace` is requested); contract Q6 resolved as the exported `OperatorFailure(message, { code?, errorData? })`. Options owned by later phases (`mode: 'report'`, `trace`, `timeout`, `useCache`, `cache`) are accepted and inert._
_Spec: evaluator-methods § One spine; Node grammar § fallback; Type § Null policy; contract § Engine guarantees._

**4.2 · First operator batch: the eager set.** Arithmetic (`plus` and friends), comparisons, simple strings (`lower`/`upper`/`trim`/`split`/`length`), `convert` (conditional nullPolicy's proof), `equal`/`notEqual`. Tests: hand-migrated v2 math/comparison/string suites + new null-gradient and no-coercion tests.
_Built (September 2026): 24 definitions as flat `defineOperator()` literals under `src/operators/` (`math`, `comparison`, `string`, `array`, `convert`; `coreOperators` in `index.ts`); `dequal` **vendored** as `src/primitives/deepEqual.ts` (Carl — zero runtime dependencies); the body-params TypeScript inference built here too (`defineOperator<const P>`, `src/inference.ts` — Carl's call to pull it forward); the empty-literal-aggregate check is validate-hook-authored (`src/operators/shared.ts`), with one known edge: a literal `[]` beside a *dynamic* `expect` reports the error, since hooks see literals only. Test files are feature-named (`test/operators-*.test.ts`, `test/null-gradient.test.ts`, `test/inference.test.ts`)._
_Register rows: 2, 3, 10, 11, 12, 13, 14. Parameter passes: batches 2–4 (eager subset)._

---

## Phase 5 — Scoping & laziness

**Phase 5 ships Batch 1 — Logic & control entire** (`and`, `or`, `not`, `if`, `match`, `firstOf`): the operator set that forced these capabilities into the ledger. `truthiness` is _not_ a delivery mode and landed in Phase 4 as `applyTruthiness`; what remains of it here is its per-element form on a settlement stream. `not` is eager — it appears in 5.3 because it completes the batch, not because it is lazy.

**5.0 · The artifact shapes, and corrections carried in (added at planning, September 2026).** `lazyElements`, `lazyEntries` and `race` need element- and entry-addressable compiled nodes, which a skeleton cannot give: its holes are _maximal_, so a partly-constant element dissolves into the enclosing shape and stops being a node at all. New `ElementsNode` / `EntriesNode`, parameter position only, built on a mode-aware path covering both the shorthand and canonical faces, and only when at least one child is non-constant — an all-constant literal stays a `ConstantNode`, which is what keeps literal arrays visible to `validate` hooks and the dead-expression warnings alive.
_Built (September 2026): `staticChecks.visit()` traverses both kinds and its switch ends in `return node satisfies never`, so a future node kind is a build error — load-bearing, because `visit` is the only resolver of the scoped namespaces and the only builder of the vars cycle graph, and a cycle routed through an untraversed kind hangs at runtime rather than failing validation. `nodeCount` restated positively (a new kind is structure until deliberately counted); `maxDepth` unified, so an expression measures the same through either face. Two defects fixed here: a `vars` block on a plain-literal root made `rootHoles` treat the root as a single hole and reported `shielded: false` although every hole carried a static fallback (fallback rule 3 says per-hole for a literal root); and engine-internal errors are now branded and cut through the fallback process, which previously served them back as the author's placeholder._

**5.1 · `vars`.** Lexical scope chain, lazy + memoized promise mechanism (shared in-flight evaluation, memoized rejections, fallback rule-5 corner), shadowing, plain-literal `vars` consumption. Tests count evaluations via spy operators — evaluate-at-most-once is the assertion.
_Built (September 2026): `src/evaluate/scope.ts`; `once()` from Phase 4 already carried the whole guarantee. A thunk binds to the scope that DECLARED it, never to whichever node demanded it first — the alternative lets an early-resolving `and` cancel a var, memoize that cancellation, and poison a later legitimate demand. One scope spans a node's parameters and its fallback; the abort scope (5.3) is deliberately narrower. Ruled with Carl: **`strictDataPaths` governs every namespace's drill, not just `$data`** — authoring-time path checking, the strongest of the assessment's three typo mitigations, exists for `$data` alone, so the runtime throw is the only protection the others can have. Follow-up: `strictDataPaths` and the code `missing-data-path` now both under-describe their scope._
_Spec: References § $vars; Node grammar § vars on plain literals._

**5.2 · Lazy delivery modes + their holders.** `lazy` (`if`, `match.default`, the `…Default` family), `lazyElements` (`firstOf`), `lazyEntries` (`match`), the degeneration rule (dynamic values → pre-resolved handles). Tests: branch-never-evaluates assertions via spies; the `…Default` semantics.
_Built (September 2026): the layers apply to a lazily-delivered value **at the moment of demand**, inside the handle — Carl's ruling, so the contract's post-everything promise holds for every mode and `and`/`or` receive plain booleans rather than calling `isTruthy` by hand. `nullPolicy: 'propagate'` is the one layer that cannot come along, and the skip had to be **enforced, not merely documented**: an unsupplied lazy parameter takes its declared default, and that default is a whole value reaching the null-policy layer, so `if.else`'s `default: null` made `{ $if: [true, 'yes'] }` resolve `null` with the taken branch never demanded. The derived reject is untouched. `inference.ts` now tests the delivery mode before truthiness (a `race` parameter declares both, and the stream is what arrives), and the container modes deliver `LazyValue<unknown>` — the declared type describes the container and there is no element-type declaration to do better with._
_Register rows: 1, 5, 6, 7, 8, 9. Contract § Evaluation modes._

**5.3 · `race`: `and`/`or`.** Parallel early resolution, per-node abort scopes, settlement streams, Kleene parking, deterministic lowest-index failure, vacuous identities. Latency-scripted mock operators make completion-order permutations testable.
_Built (September 2026): per-node abort scopes are a **general rule of the runtime interface, not an `and`/`or` quirk** (Carl's ruling) — a node derives a controller when any parameter is delivered as a handle, and settles it the moment its body settles; Phase 10 composes the deadline into the same chain. Specifics that had to be right: the abort scope covers parameters only, not the fallback (which runs after the body settled and would be refused at its first node boundary); cancellation is not failure, so it travels the same bail-out as an engine bug and reaches no `fallback`; `rootSignal` distinguishes a scope abort from the kill switch; an element's promise resolves to a `Settlement` and never rejects, so an abandoned operand cannot become an unhandled rejection — the handler is attached where the promise is created, the only point early enough to matter. The scope chain is hand-rolled rather than `AbortSignal.any`, whose browser floor (Chrome 116, Safari 17.4) is higher than this package should ask for. Honest about reach: JS cannot interrupt running code, so an abandoned subtree completes and its result is discarded; what the abort reliably stops is work not yet started, and anything holding the signal — the I/O clients, where the time lives._
_Register row 4; worked example 5; implementation notes § race with error parking._

**Deferred out of Phase 5, recorded (September 2026).** Register row 9's agreed `validate()` lint — warning when `$not` sits directly over a propagate-family node — cannot be written: an operator's `validate` hook receives _literal_ parameter values only, so a hook on `not` never learns that its `value` is a `$greaterThan` node. `not`'s runtime behaviour is correct and tested without it. The same root cause holds Phase 4's recorded `plus`/`expect` edge, and both want the same fix: an additive third hook argument describing node-valued parameters. Carried to the authoring-polish phases.

**Raised and parked at 5.3 (September 2026, Carl).** Whether `and` / `or` should get a symbolic alias was re-opened — the Operators area recorded an outcome but no argument — and settled the same way: they stay alias-free, because alias-free is the reversible direction, because `match` and `firstOf` are bare too (so there is no inconsistency to fix), and because rule 0 favours the plain words. Reasoning and the note that any future symbol should be `&&` / `||` rather than `&` / `|` are recorded in the Operators area of [v3-api.md](v3-api.md).

---

## Phase 6 — Iterators

**6.1 · `perElement` + bindings.** `$element`/`$index`, `as` renaming and its scope boundary, per-index memoized child scopes; `nullInputDefault`; decider iterators reusing the race machinery.
_Built (September 2026): the phase turned out to be **runtime-only** — Phases 2, 3 and 5 had already landed the `perElement`/`over` registration checks, the structural `as`, the binding frames, `$order`/`$orderIndex` recognition, the binding-scope walk order and `resolveBinding`, with a green static suite over all of it. Three holes remained: the delivery in `src/evaluate/params.ts`, `$element`/`$index` in `src/evaluate/reference.ts`, and the definitions. The binding chain (`src/evaluate/bindings.ts`) deliberately mirrors the vars chain next door but stays a **separate chain**, because the lifetimes differ — a vars scope spans a node, a binding frame spans one element. The handle is built in **pass 2**, not pass 1, since it closes over the `over` sibling's VETTED value: `layerOrder()` puts the target ahead of its dependant, which is what lets `nullInputDefault` replace a null `input` before the derived reject sees it (that parameter needed no engine work at all — the Phase-4 `replacesNullAt` layer already ran at the right moment). Ruled at planning (Carl): **`PerElement.settle()`** — the deciders get their settlement stream from the engine rather than building one, so "parked, never thrown" stays an engine guarantee and Phase 12 has one place to hook per-element statuses; `race.ts`'s private `streamOf` generalized to `indexedStream(count, run, vet)`, of which `raceStream` and `settledStream` became callers. `perElement` runs the **full vet per index** (not truthiness alone, as for the container modes) because its declared type describes the element, not a container — the contract's "When the layers run" paragraph had omitted it. With the last mode built there is no unbuilt mode left for a test to reach, so the delivery switch is now exhaustive by `satisfies never`: an eighth mode is a **build** error, which is strictly stronger than the runtime test it replaced._
_Register rows: 23, 24. Parameter passes batch 5._

**6.2 · The five operators.** `map`/`filter`/`find`/`some`/`every`.
_Built (September 2026): all five in `src/operators/array.ts` beside `length`. `decide()` moved out of `logic.ts` into `shared.ts` and is now shared verbatim by `and`/`or` and `some`/`every` — the quantifiers really are batch 1's machinery with the condition list factored into data. Its sibling `collectAll()` serves the transforms, applying the same lowest-index rule to failures (evaluator-methods' "iterators likewise") and raising as soon as that index is **known** lowest, so determinism costs no wait on elements that cannot change the answer. `find` is the one bespoke body: order-aware early resolution, resolving at the first truthy index once every earlier index is known falsy. The shared parameter block is spelled as `as const` objects rather than built by a factory — a factory's return type widens `evaluation` to `string` and the inferred body types collapse; with the const spelling the five bodies need **no casts at all**, which is the inference machinery earning its keep. `src/dev/demoOperators.ts`'s `map` stand-in had to go, not merely could: `inspect.ts` registers it beside `coreOperators` and the names would collide._

**6.3 · Static checks, docs and the close.** The dead-binding warning; the out-of-scope `as` error; the generated reference.
_Built (September 2026): two checks batch 5 promised but nothing implemented. `dead-binding` rides the `IteratorFrame`, marked when a reference resolves against it and warned on pop. The out-of-scope `as` fix could not be done during the walk — depth-first meets an iterator's `input` before its `as` — so the compiler collects every declared `as` name and upgrades the matching unrecognized-`$` warnings to `unresolved-binding` errors afterwards, replacing in place so the issue stream keeps tree order. `codegen/buildOperatorReference.ts`'s PENDING `iterators()` block was deleted (the build fails while a pending entry names a registered operator). The showcase scaffolding that Phases 4 and 5 had duplicated verbatim is extracted to `src/dev/showcase.ts` rather than copied a third time._

---

## Phase 7 — Remaining core operators (core-complete milestone)

**7.1 · Data & objects.** `get` (path resolver reuse, `missingPathDefault`, `from` + the `EvaluationData` sentinel), `buildObject` (duplicate-key semantics + warning).
_Built (September 2026): both in `src/operators/data.ts`. Almost nothing in the engine had to move — the `EvaluationData` sentinel, the `replacesNullAt` layer, the `lazy` handle and the `elementShape` constraint were all already load-bearing elsewhere, and `get`'s whole path grammar (quoted segments, the segments array, `[*]`) had shipped with the Phase-1 resolver. Two things were genuinely new. `get` reads `strictDataPaths` off **`context.options`**, the only channel a body has to an option, so the sugar contract's "same strictness response" became one flag consulted once rather than two layers agreeing by coincidence (it arrived declared, via `readsOptions`; that declaration was dropped at the Phase-8 opening — contract open Q10 — leaving the read as it was); and the compiler gained **`get`'s dependency recording** (obligation B6), which was specified but never built — a literal `path` with no `from` joins `dataPaths`, a computed one sets `dynamic`, a supplied `from` contributes neither. Batch 6's three unruled `[*]` sub-questions all closed on the built resolver rather than by decision: chained projections nest, `missingPathDefault` fires on a whole-path miss only (a per-element miss is already a `null` slot in a found result), and the segments-array form cannot spell a projection at all. `from` declares `any`, not the contract example's `['object', 'array']` — its `value` policy is declarable only where the type names null._
_Register rows: 25, 26. Batch 6._

**7.2 · Renderers & regex.** `buildString` (token engine, literal-face validate hook, unbound-token rendering, `closeGaps` over the segment list), `join`, `regex` (modes, compiled-literal-pattern reuse, `noMatchDefault`), `literal` (mostly compile-side already — confirm end to end).
_Built (September 2026): the token grammar is one scanner in `src/templateTokens.ts` with two consumers — the compiler and the body — so it cannot drift. Four rulings shaped it. **Reference tokens desugar into `substitutions`** (Carl): a literal `{{$d.first}}` adds the compiled reference to the substitutions object under the token's own text, leaving the template byte-unchanged, which means the body needs no reference machinery at all and `nullValueDefault`, the static scope walk and dependency recording all reach reference-token values for free. It needs an object to grow, so it was **narrowed to the named and no-substitutions faces**; beside a literal array or a dynamic map the token is unrecognized, renders itself, and warns. **The literal-face findings are warnings, not the errors the pass drafted** — an error refuses the expression, which would have made a percent-encoded URL in a positional template unevaluable and stopped register row 15's own example from rendering. And they are emitted by the **compiler**, not a `validate` hook, because the desugar turns `substitutions` into a skeleton and hooks see constant parameters only. `regex` came out as a pure rename of v2 plus everything new, with **artifact obligation B5 deferred**: nothing can write the precompute slot, and a `RegExp` the engine already caches by source and flags is not worth a definition-level hook — constructing per use is also the only `lastIndex` story that needs no care. `closeGaps` runs over the segment list with the run stripped in place, which is what makes "each run consumed at most once" fall out instead of needing bookkeeping. One wording correction went back to the pass: zero-length matches advance one UTF-16 unit, and one code point only under `u` — "the JS `matchAll` rule" and "advance one code point" are the same thing only when the flag is set, and the batch's code-point unit ruling does not reach inside a pattern. `literal` needed no code: Phase 3 had built the boundary, and what Phase 7 added is the evaluation-level proof that a quoted payload comes back by identity and nothing downstream re-captures it._
_Register rows: 15–19, 21, 22. Batch 4._

_Review note (September 2026): `buildString` and `get` are the first operator names hard-coded into `src/compile/compile.ts` — each a hook the walk calls unconditionally that bails on a name check. Accepted on the same grounds as the deferred regex precompute slot: a definition-level hook is more machinery than two call sites buy. A third such hook should prompt a dispatch table keyed by operator name rather than a third bail-out._

**Milestone: all 41 core operators live — 40 definitions plus `literal` as grammar; the bulk of hand-migrated v2 expression tests pass.** Worked examples 1 (minus http) becomes a passing test here.

---

## Phase 8 — The instance layer, completed

**8.1 · Full options semantics.** The two-level merge rule (all the consequence-table cases — the merge itself landed in 4.1 as `mergeOptions`; this chunk proves every table row), frozen per-evaluation context, never-mutate-the-instance, `updateOptions` + registry re-validation, the `useCache` modifier default (parameter defaults and the default-`fallback` catch landed in 4.1; the chain is built here but has no consumer until 9.1 — see the built-note), `getOptions` snapshot.
_Spec: Options, entire._
_Built (September 2026): an instance's mutable world is one `InstanceState` record in [src/FigTree.ts](../../src/FigTree.ts) — options, the registry and the compile cache — swapped whole, because the three are derived from each other and an artifact bakes registry resolution in. One builder serves construction and `updateOptions` alike, so validate-before-swap is structural rather than guarded: `buildRegistry` throws from inside it, so a caller can only ever assign a complete record. Much of the chunk was pre-landed; what it added was the two methods, the re-validation, and three fixes. **The registry keys never leave the instance** (Carl): bodies receive the whole options object, and leaving `operators` in it would hand every body an array of live definitions — and from Phase 9 the clients closed inside them. The first cut held them apart in a private `RegistrySource`; the Phase-8 review collapsed that to one strip at the two escape points, `getOptions()` and the evaluation context, which is what lets `EvaluationOptions` (the options type minus the two keys) be true by construction. `getOptions()` already excluded both, so instance and introspection now agree. **A live defect**: `mergeOptions` tested `isPlainObject`, which an `AbortSignal` satisfies, so an instance-level signal combined with a per-call one was spread into an object that is no longer a signal and threw at the first node boundary — a supported combination (the option's own doc line calls it the default for all evaluations) that no test had ever set. The merge tests `isPlainDataObject` now, so signals, stores, `Date`s and class instances replace as a unit. **Two aliasing holes**: the merge stored an incoming block by reference where the instance had no counterpart, and construction stored the caller's blocks outright; both copy at the merge rule's depth now, which is what makes the per-evaluation freeze safe to deepen to block level — every block is instance-owned by the time it is frozen. Level three stays shared and unfrozen, deliberately: a caller's data values keep their identity because results may reference them. **`rejectPerCallRegistry` tested key presence**, so `{ operators: undefined }` threw while the merge rule three lines away ignores undefined; it tests by value now. `effectiveUseCache` (src/evaluate/operator.ts) is written and **deliberately uncalled** — working rule 3; Phase 9.1 wires it into the result cache. Divergences recorded back: the merge rule's plain-**data**-object wording, the missing `operatorDefaults` consequence row, the snapshot depth, and the removal position (issue #157). `test/v2-working/18_optionHandling.test.ts` retired — two merge cases migrate onto `data`, five die with `excludeOperators` (two re-expressing as registry replacement), one dies with the functions tier, and the snapshot case migrates **inverted**, v2 having replaced `data`/`fragments` wholesale where v3 merges them._

**8.2 · The compile cache.** Two layers (identity `WeakMap` + bounded content LRU, re-register-on-content-hit, opaque-constant identity-only guard), invalidation by exactly `operators`/`fragments`/`operatorDefaults`. Observable via a spy `validate` hook counting compiles — no internals in assertions.
_Spec: implementation notes § two-layer compile cache; worked-examples lifecycle steps 3, 4, 6._
_Built (September 2026): three modules — [src/lru.ts](../../src/lru.ts) (domain-free, so Phase 9.1's default store is its second consumer), [src/compile/contentKey.ts](../../src/compile/contentKey.ts) and [src/compile/compileCache.ts](../../src/compile/compileCache.ts) — with the cache held in the instance state, so **invalidation is that field being replaced** and there is structurally no clearing API for Phase 9's `clearCache()` to reach. Three things the build settled. **The key is the serialized string, not a hash** (Carl): hashing does not replace serialization but follows it, so it is strictly more work at both ends, and a `Map` already hashes a string key natively and then verifies with full equality — where a hand-rolled digest would swap a verified hash for an unverified one whose collision serves the wrong artifact. The notes' "hash of the JSON-serialized input" is amended. **The serializer is not `JSON.stringify`**, which would throw on a cycle, honour `toJSON`, and flatten `NaN`/`±Infinity`/`-0`/`Date`/`Map` — each of those either crashes the lookup or lets two different inputs share a key. It refuses anything non-plain instead, and that refusal turns out to be the **load-bearing** half of the identity-only pair: obligation C5's flag is set only for values the compiler walks, and a `literal` payload is never walked, so an opaque value inside one slips past it while the serializer catches it. Both guards ship and the docs now say which is which. **The probe and the cache reconcile through the entry type** (Carl): `evaluate()` short-circuits constant inputs before any artifact exists, so a constant container never produces one to cache — a cache entry is therefore an artifact **or** a memoized inert verdict with its depth, which is what makes the notes' "a hit turns that O(size) walk into a pointer lookup" true, and what keeps the verdict invalidated by the same three options, since the probe consults the registry. Ordering falls out well: constant containers exit at the probe, so the O(input) serialization never runs on inert data at all. Bound: 200, internal, no option — the identity layer already carries the hot set unbounded and free, so the content layer only covers fresh arrivals, and a full cold recompile of the largest reported working set is 0.33 ms. `validate()` stays out of the cache altogether — it neither reads nor warms it (ruled at the Phase-8 review: an authoring tool's report should cost a compile, and reading the cache would have needed a second answer shape, since an inert verdict cannot carry the unrecognized-`$` warning a full artifact does); the first cut shared the cache and upgraded inert entries in place. **Narrowed at implementation**: the first cut rebuilt the cache on every `updateOptions`; an update naming none of the three keys now carries both registry and cache across untouched, which is what makes the invalidation set exactly what obligation C1 says._

---

## Phase 9 — Result caching & I/O

**9.1 · The result cache.** Store/TTL/maxSize, `auto` keying, `context.cache.memo` goes live behind the existing stub, failures-never-cached, `clearCache()`. Recording-store tests.
_Spec: contract § Caching; Options § Caching._
_Built (September 2026): two modules, split the way the spec splits the idea — [src/resultCache.ts](../../src/resultCache.ts) is storage plus policy and knows nothing about nodes, [src/evaluate/memo.ts](../../src/evaluate/memo.ts) is the caching semantics. Five things the build settled. **`CacheStore` widens to four methods** (`get`/`set`/`delete`/`clear` — Carl): at two, the engine cannot do what the spec says it owns, because `clearCache()` needs `clear` and freeing an entry found expired or invalidated at lookup needs `delete`, so on a host-supplied store both would have been silent no-ops. The mutators are typed `void` rather than `void | Promise<void>`, which is what lets a plain `new Map()` be passed straight in — TypeScript's void-return rule accepts any return type there, and the engine awaits whatever comes back, so an async store still works. **`maxTime` is seconds, fixed from the write, default 1800** (Carl): v2's units and default kept, but not v2's sliding window, because what is cached is a remote read and its staleness is a property of the world rather than of access frequency — under a sliding window a dashboard polling every 10s never expires its entry. Cold-entry reclamation is `maxSize`'s separate job, and it is the **built-in store's** job alone: the default store is an `Lru` sized by `maxSize`, and a host-supplied store keeps its own bound (ruled at Phase-9 review, September 2026, Carl — the first cut kept an engine-side key ledger so `maxSize` applied to every store, which deleted a Redis-backed host's entries past fifty with no way to opt out). **A generation counter guards `clearCache()`**, which is specified sync while `store.clear()` may not be: the counter is bumped and any envelope written under a lower generation reads as a miss — which also discards a result computed before the clear and written after it. **The `'auto'` layer refuses more than the spec says.** Its literal reading — "keyed on the resolved eager parameters" — is unsound for an operator that mixes delivery modes, since two `if` nodes with one condition and different branches would collide; so a definition with `deliversLazily` takes no auto layer at all. That one test is also what keeps a settlement stream — a plain-prototype object whose only own string key is `length`, which the serializer **accepts** — out of the key; a value-level `isEngineHandle` sweep behind it was dropped at Phase-9 review as unreachable, since every value past the test is an eager result already through the escaped-handle guard or an authored constant. **No single-flight**, recorded so it is not re-litigated as a bug: a shared in-flight promise carries whichever node's abort signal created it, and [src/evaluate/scope.ts](../../src/evaluate/scope.ts) already records that ruling for `vars`. Also: `normalizeResult` runs INSIDE the memo unit, so the finite and escaped-handle guards are uncacheable and a cached value can never be `undefined`; a per-call `cache` block is **rejected** like `operators`/`fragments` (Carl), since the store is instance-lived; and the result cache lives beside `InstanceState` rather than in it, because that record's own header says its fields may only swap together and this one must survive precisely the rebuild that destroys the compile cache. A per-node TTL is [issue #162](https://github.com/CarlosNZ/fig-tree-evaluator/issues/162) — the envelope already carries a per-entry `expiresAt`._

**9.2 · Clients & factories.** `HttpClient`/`SqlConnection` contracts, `FetchClient`/`AxiosClient`, SQL wrappers, `httpOperators(client)`/`sqlOperators(connection)` factories. Contract-conformance tests run against the mock client too (it must satisfy the same interface — contract Q8's check). **Start from v2's `httpClients.ts`/`databaseConnections.ts`** (Carl, July 2026) — the one sanctioned code port, being engine-independent: reshape to the single `request()`/`query({ text })` entry points, add `signal` passthrough, throw `OperatorFailure`, drop the query-string assembly (operator-owned in v3), the `getHttpClient` window-sniffing, the `console.log`s and the node-fetch types; the axios/fetch error-payload extraction carries over nearly verbatim.
_Built (September 2026): `src/clients/` — `http.ts`, `sql.ts`, and `failures.ts`, which is the one place an `errorData` payload is assembled and takes **no headers argument at all**, so "header values never echo" holds by construction rather than by review. **Classes, and both SQL wrappers ship** (Carl — packaging Q2 resolved): `new FetchClient()`, `new AxiosClient(axios)`, `new PostgresConnection(client)`, `new SQLiteConnection(db)`, methods as arrow fields so a torn-off `request` keeps its client. The drivers' types are **not** imported — that would put `axios`/`pg`/`sqlite` into the emitted `.d.ts` and demand them of every consumer — so each wrapper declares the minimum structural shape it calls, and `test/clients-types.test.ts` assigns the real imports to those shapes so an overload change is caught here rather than by a consumer. Four v2 defects were fixed rather than carried: `.json()` ran **before** the `ok` check, so an HTML 502 came back as a parse error (v3 reads text, then classifies); no `signal` reached either client; a transport error was wrapped in `new Error('Network error: …')`, destroying the `AbortError` name the engine now classifies on; and the axios failure is built from `config.url` alone, never `config`, which carries the request headers. `PostgresConnection` **refuses an object `values`** rather than binding nothing silently, node-postgres having no named-bind form. SQL `errorData` carries `{ driver, code, table, constraint }` and deliberately not pg's `detail`/`hint`, which quote row values into a payload a host may log. One divergence recorded: **`AxiosClient` cannot enforce the JSON-first rule**, because axios parses before the wrapper sees the body and a non-JSON response is indistinguishable from a JSON string — so the shared conformance table covers what every client can promise, and JSON strictness is asserted of `FetchClient` alone. **Q8 is discharged**: `test/clients-conformance.test.ts` is one scenario table run over `FetchClient`, `AxiosClient` and `MockHttpClient`, with injected stubs rather than module mocks, so nothing in the default suite touches the network._

**9.3 · The I/O operators.** `http`/`graphQL`/`sql`: effective-request manual memo keys (`returnPath` outside the key), wire-null rules, `requestTimeout` composition into `context.signal`, `errorData` (header names only), mutation/injection lints. Mock-client tests are primary (fetch counts — the lifecycle example verbatim); live-network and Northwind SQL tests tagged and optional.
_Register rows: 27–30. Batch 8; ledger #15–17._
_Built (September 2026): three `defineOperator()` literals plus both factories in [src/operators/io.ts](../../src/operators/io.ts), over the shared assembly in `ioHelpers.ts`. One generating rule keeps the three agreeing — **the memo unit is everything that can fail because of the network or the driver; pure reshaping of a successful payload sits outside it** — which puts `returnPath`, `shape` and `noRowDefault` outside the key and graphQL's `errors` check *inside* it, since a 200 carrying errors is a failure and "failures are never cached" would otherwise be false. Rulings and corrections: **`sql.shape` is `'rows' | 'firstRow' | 'column' | 'firstValue'`** (Carl), retiring the drafted `'row'`/`'rows'` near-miss, where a one-character typo silently changed the result shape; **`http.body` excludes null** in its declared type, because register row 27 depends on that and `any` would have delivered `null` and POSTed a JSON null document; **`Content-Type`/`Accept` are operator-supplied at the bottom of the header chain**, so they appear in the key, can be overridden, and can be removed with `'Content-Type': null`; **headers are sorted in the key and only there**, a header map being a set rather than a sequence; **empty containers normalize out** before keying, since the rest slice binds `values: []` where the named face leaves it absent — the same request, two keys otherwise; and `strictDataPaths` does **not** reach `returnPath`, a response being operator output rather than a reference namespace. **The mutation and injection lints are dropped** (Carl, reversing the July 2026 batch-8 ruling): POST is admitted precisely because reads sometimes need a verb with a body, so v3 cannot police mutating HTTP, and singling out SQL writes is inconsistent on its face — the enforcement that works is a read-only role on the connection, which is now stated in `sql`'s own `description` where a host meets it. The engine work this chunk needed turned out to close two live gaps: **`requestDeadline`** (src/evaluate/context.ts) races a promise against the body rather than trusting the signal alone, because a driver that cannot abort would otherwise hold a node past its deadline forever; and `classifyBodyFailure` (src/evaluate/operator.ts) splits kill switch / cancellation / per-request expiry, which nothing had needed before because **no core operator had ever held the signal**. Two things were simply broken until something did: an abort landing while parameters resolved was missed entirely (a listener attached to an already-aborted signal never fires), and an `aborted` error was catchable by `fallback` although the contract says the caller's signal cuts through every one. Both fixed here. One spec correction: batch 8 says `{ $http: … }` on a clientless instance is an unknown-operator **error**, but the shorthand face cannot be one without making every `$`-keyed data object an error — it is an `unrecognized-identifier` **warning** and the object passes through as data; the canonical face is the error. Also recorded: `validate` hooks see literal values only, so the `body`-on-a-GET lint fires on a literal `method: 'get'` alone — an absent `method` and a dynamic one are indistinguishable there, and refusing the dynamic case would refuse a legal expression. The runtime check covers both._

_Also at the close: the `http` stand-in in `src/dev/demoOperators.ts` had to go, since `inspect.ts` registers the demo set beside `coreOperators` and the names now collide (`clamp` stays, and the file with it); `codegen/buildOperatorReference.ts` builds its live set over a stub client so the three operators are documented from the code rather than from `PENDING`; and **the whole test tree is type-checked for the first time** — `pnpm typecheck`, now a CI step. Nothing checked it before: ts-jest is transpile-only under `isolatedModules` and `tsconfig.json` includes `src` only, which is how three assertions in `inference.test.ts` came to be quietly false. All three were stale rather than wrong-in-the-engine, and they are fixed. The same pass found and fixed a **latent public-API defect**: `defineOperator`'s single signature over `OperatorDefinition<P> | ValidatedOperatorDefinition` stopped contextually typing a literal that carried enough of the validated shape's fields — `useCache` and `cache` together were enough, which is exactly the pair every I/O operator declares — so every body parameter silently became `any`. Overloads replaced the union._

**Milestone: the full lifecycle worked example passes as a test, fetch counts and all.**

---

## Phase 10 — Kill switch & shielding

**10.1 · `timeout` + `signal`.** Strict whole-evaluation deadline, abort threading through the scope chain into clients, cancellation at node boundaries, shielded assembly on timeout (splicing the artifact's precomputed static fallbacks), signal-rejects-always. The `validate()` badge already exists (3.3); this makes it true. One case Phase 9 left open belongs here: a body holding a driver that ignores its signal, with no per-request `timeout`, is only abandoned when the driver returns — the race against the composed signal that `requestDeadline` sets up exists only when a node declares a timeout. The whole-evaluation deadline is the natural point to race every I/O body against its signal regardless.
_Spec: Node grammar fallback rule 3; worked example 3 verbatim._
_Built (September 2026): three files, one concern each — the **mechanism** in [src/evaluate/abort.ts](../../src/evaluate/abort.ts) (`childScope`, `deadline`, the reason markers; the Phase-9 `requestDeadline` becomes a one-line specialization of `deadline` with its reason made a parameter), the **meaning** in `src/evaluate/internal.ts` (`abortedOutcome` classifies the root's abort *by reason* — the timeout marker, "settled", or anything else meaning the caller; `isKillSwitch` covers `timeout` as well as `aborted`, closing a hole where a `timeout` error could have been caught by a fallback), and the **policy** in [src/evaluate/run.ts](../../src/evaluate/run.ts), which `FigTree.evaluate()` calls in place of the bare `evaluateNode` — wrapping the recursive evaluator, not forking it; every node still goes through `evaluateNode`, and the wrapper's whole involvement is the two classification calls it already made. **The root scope exists for every evaluation** (Carl, at planning): the caller's `signal` as parent, the `timeout` as timer, settled with the evaluation like any node scope — so an uncaught failure cancels its siblings' in-flight requests kill switch or not (the evaluator-methods throw-mode sentence, true unconditionally), and a body's `context.signal` is aborted once the call has returned; two suites had asserted the opposite after the fact and were rewritten around an in-body probe. **Shielded assembly is a race per hole** against the root's expiry, which hands back the static fallback where a hole has not yet won; microtask-before-macrotask ordering, with the expiry's rejection the first reaction queued in the dispatch, makes the finished/unfinished cut exact, and nothing is evaluated after it. **The unshielded case is the same race** over the whole evaluation, so the caller always receives the root-level error (path `[]`, naming the budget) and a driver that ignores its signal cannot hold the call — the Phase-9 open case, closed. Caught at planning review, before shipping: a pre-aborted caller signal is answered once at the top of the run, so the error is the root's rather than the first hole boundary's; a comment-only or vars-only literal is not inert yet has no holes, so `shielded` is vacuously true and it takes the plain path; `setTimeout(fn, undefined)` fires at once, so a signal-only evaluation arms no timer; and `Deadline.settle()` detaches its own expiry listener before aborting, so settling never builds an error nobody sees. Readings: the clock starts after the static gate; a post-deadline success is still cached, writing not being evaluation; `timeout` is a positive finite number of ms or absent, checked at construction, `updateOptions` and per call (`invalid-options`); `signal` must be an `AbortSignal`. Worked example 3 is a test verbatim, with the mock's latency shortened and one addition — a cache hit cannot time out, so the un-shielding step clears the result cache first. Report mode's two rows are `it.todo` for Phase 12, which attaches where a hole takes its fallback in `run.ts`._

---

## Phase 11 — Fragments

**11.1 · Registration and the static layer.** Body compilation at registration (rides the Phase-3 compiler), signature validation, cycle detection, batch semantics, replacement re-validation. Plus the rollups a call site needs from its target, all computed here in reverse topological order (the cycle ban makes the graph a DAG): transitive `nodeCount` (a total, so a call site adds its target's, twice for two call sites) and `maxDepth` (a maximum, so `max(current, depthOfCall + target.maxDepth)`), `dependencies`, `identityOnly`, and the body root's static fallback, which a call node with no `fallback` of its own lifts for timeout shielding (obligation B2, amended). **Call-site static checks belong here too** (moved from 11.2 at implementation): a body calling another fragment needs them, so they cannot wait for the runtime chunk — which makes the split static/runtime rather than registration/calls.
**11.2 · Calls.** `$params` on the vars mechanism (lazy memoized args, caller-scope closure), both argument modes, fragment shorthand faces end to end. Includes the bare namespace forms (References): bare `$params` is the declared parameters resolved, and so demands every argument; bare `$vars` becomes an explicit error with its own code. _(Phase 3 had already made both bare forms explicit `invalid-reference` errors, so the delta is a code and a message on one side and legality on the other — not the accidental empty-name report this entry once described.)_
_Spec: Fragments, entire; worked example 4._

_Built (September 2026): two modules and one contract change. [src/fragments.ts](../../src/fragments.ts) is registration — a four-pass builder (shape/names/declarations, then bodies, then cycles, then rollups) whose pass order **is** the batch-semantics rule: every entry exists before any body compiles, so a body may call any fragment in the batch regardless of key order, and the same wholesale rebuild serves replacement re-validation and an `operators` change with no special case. [src/evaluate/fragment.ts](../../src/evaluate/fragment.ts) is the runtime, written against `evaluateOperator` so the two read as siblings. The contract change is that **fragments join `OperatorRegistry`** rather than riding a second lookup map, which is what the registry header always said they would do: one `claimed` map now guards operator names, aliases and fragment names together (discharging [#136](https://github.com/CarlosNZ/fig-tree-evaluator/issues/136)), and `compileExpression`/`probeConstant` lose their separate `fragments` parameter._

_Three things the build found. **Sealing needed no runtime check**: a body evaluates under a context with `scope` and `bindings` dropped, and both lookups already terminate on `undefined`, so a body structurally cannot reach a caller frame — and the static half was already enforced by compiling bodies in isolation, where an unbound binding or var is an error. **Anchoring the two-level path has exactly two sites**, not one per error construction: the call boundary anchors with its own frame, the argument thunk with the caller's, and a brand stops any outer boundary re-anchoring — which is what makes "nested calls report the innermost body" and "an argument failure is caller-side" fall out of one mechanism. **The rollups cannot be folded during compilation**, because a body compiled before its callee would compose against zeros; the walk records each resolved call site with its depth instead, and `composeRollups` is applied twice — at artifact assembly, where targets are final, and again at registration in reverse topological order. The two applications never feed each other: the artifact carries its un-composed measurements as `own` beside the composed ones, and the fold composes from `own` — so a change to the compose order cannot double-count (the review of the Phase-11 PR caught the fold reading the already-composed fields, safe only because every target read zero during pass 2). Rulings recorded in the Fragments area: eager dynamic-mode signature checks, the strict definition shape, dependencies rolling up at the call site, and the shorthand face's warning-not-error consequence. `getFragments()` stays in Phase 13.1 (Carl, at planning), so body warnings are stored and asserted white-box against the builder, the `test/registry.test.ts` precedent. `identityOnly`'s rollup is kept per the plan but is **conservative rather than required**: one registration means one body object, so no content-key confusion is possible through a call._

---

## Phase 12 — Report & trace

**12.1 · `mode: 'report'`.** Hole-boundary degradation, error collection in tree order, `holePath`, static-errors-under-report, the throw/report invariant tests, timeout-under-report shapes, `related` parking attachment. Worked example 1 in full becomes the acceptance test.
_Built (September 2026): one new module — [src/evaluate/report.ts](../../src/evaluate/report.ts), the collector — and one structural change that the phase turned on. **`run.ts`'s three paths collapse into a single hole boundary** (Carl, at planning): report and a shielded timeout both want something wrapped around each root hole, so `run.ts` composes one function from the mode and the deadline and stops applying it, and the root skeleton applies it instead (a node root's single hole being the root, the boundary wraps the call there). `evaluateShielded` dissolves into it. The composition order is the whole of the semantics — **the expiry race sits OUTSIDE the report catch**, because a kill switch ends the evaluation where an ordinary failure only degrades a hole — and the mechanism is untouched: still a race against `root.expiry`, still with `deadline()`'s listener registered first, so Phase 10's microtask-before-macrotask argument holds verbatim. The payoff beyond composition is Phase 12.2's: every mode now goes through `evaluateNode(artifact.root)`, so the trace root is a real entry rather than something synthesized. **Tree order needed no path comparison**: every compiled node already carries `order`, its preorder position in the compile walk, so the collector sorts on a number. **`holePath` copies at a second hole** (Carl, at planning): a memoized `vars` rejection is one error object that two holes can both meet, and tagging in place would have put it in `errors` twice under one `holePath` — the first hole crossed owns it, any later one takes a shallow copy. **`related` is `decide()`'s** — the only place that ever holds the whole parked pile — and so covers the quantifiers as well as `and`/`or`, in both modes; `collectAll()` is structurally exempt, raising as soon as an index is *known* lowest and so never holding a pile. Readings: a static failure under report is **one error per error-severity issue with no `issues` array**, that array being throw mode's device for the single error it throws; the inert fast path **keeps its probe** (report wants an envelope, not nodes) and reports its `maxDepth` breach rather than throwing it; and a shielded run detects its own expiry by reading `root.signal.reason` before `settle()`. `test/v2-working/15_errorsAndFallbacks.test.ts` is **mined, not retired**: its four `returnErrorAsString` rows die with the option and their v3 successors are in `evaluate-report.test.ts` (a static failure reported rather than stringified; a caught fallback contributing nothing to `errors`), but the file's other twenty-one cases are Phase 4's fallback territory and Phases 7 and 9's error messages, so retiring it here would claim coverage this phase did not write. One divergence went back to the worked examples: under fallback rule 4 the error's `path` names the **fallback node**, not the hole root the example prints, because `wrapFailure` is first-tagger-wins — the rule that makes deep paths work at all — and the pair is better for it, `cause` naming the primary and `path` the backup that also failed._
**12.2 · `trace`.** Instance-tree assembly in the node-boundary wrapper, statuses (incl. `skipped`/`cancelled`), `trace.note` goes live behind the stub (operator bodies have been emitting since Phase 4), reference/var entries, `error.trace` in throw mode, the envelope + TS conditional return types.
_Built (September 2026): two modules — [src/trace.ts](../../src/trace.ts) for the public vocabulary and [src/evaluate/trace.ts](../../src/evaluate/trace.ts) for the recorder — and the bookkeeping lands in **`evaluateNode`'s dispatch, not in the two node wrappers**: `evaluateOperator` and `evaluateFragment` are deliberate siblings, and the dispatch is the one place every kind passes through, references and constants included. Three things the instance tree needed that appending a child does not give. **Structural order is computed, not observed** — parameters start concurrently and lazy ones are demanded late, so children are held aside while an entry is open and materialized sorted at settle, by the node's `order` then by element index. **`skipped` is an absence, so it has to be derived**: on settle, every static child with no instance becomes a leaf, over a `staticChildren` that flattens `elements`/`entries` parameters, since those are consumed by parameter resolution and their members attach directly to the operator. And **`finish()` closes whatever is still open as `cancelled`**, which is what a hole cut off by a shielded deadline actually is — its promise may never settle at all. Two things cost nothing because earlier phases had already made them true: a var's entry lands at its **declaration site** with no special handling, because `pushVars` builds its thunks over the declaring context; and a body entry's `source` is a read of the `FragmentFrame` Phase 11 already threads. **The `const` type parameters the plan called for are not needed and would have hurt** — see the TypeScript story in [v3-evaluator-methods.md](v3-evaluator-methods.md); the twelve-case matrix is `test/evaluate-types.test.ts`, checked by `pnpm typecheck`. **One live bug the phase introduced and caught**: `Promise.race` does not unsubscribe the loser, so the shielded boundary's expiry handler runs for *every* hole when the deadline fires, winners included — harmless while it only returned a value nobody reads, wrong the moment it also emitted a `shielded-fallback` event, which it then did for holes that had contributed real values. The winners are tracked and skipped, on the same microtask-before-macrotask reasoning that makes the cut exact. Vocabulary settled here, per the spec's own invitation: a `skeleton` **kind** (a container whose holes are being filled is neither an operator nor an inert literal, and an editor painting the tree needs the difference), `warnings` on the root entry for the compile-warning echo, a `gap-closed` render event for `closeGaps`, and a `query` event for `sql` carrying the statement text and **never its bound values** — the reasoning that keeps pg's `detail`/`hint` out of `errorData`. `request` (http and graphQL) carries header **names** only, by construction. Two readings recorded: a run the static gate refused has **no** trace, nothing having been instantiated; and the requirements table's `failed-discarded` needs no status of its own — a parked failure that lost its race is a `failed` child under a parent that succeeded, and the two facts together say more than one status could._
_Spec: evaluator-methods §§ report, trace, return shapes — signed off July 2026; the former spec gate (open questions 1–3 + 11) is discharged. Contract open Q9 (`context.trace.note`) is discharged here._

---

## Phase 13 — Introspection & surface completion

_Smaller than Phases 9–12 by construction: seven of the eleven surface rows are already built, and most of what remains is a **read** of data earlier phases compute. Two chunks, split design-work from reads. Planning rulings were settled with Carl in September 2026 and written back to the specs before the build — the two entries below say what to build, the specs say why._

**13.1 · `getOperators()` / `getFragments()` + the `category` field.** The snapshot types and their assembly: declarative half verbatim, `operatorDefaults` reported beside the authored values as `instanceDefault` / `instanceUseCache` / `instanceFallback` (presence by `Object.hasOwn`, so an override _of_ `null` is distinguishable), capability flags, registration order, the three-tier copy posture. `getFragments()` adds the stored body `warnings` and the registration-time `dependencies` rollup, and still withholds the body. **The `category` field is a contract change riding this chunk**: required on every definition, closed set of eight, registration error on an unknown value — which means a mechanical sweep of roughly 104 call sites (35 definition literals in `src/operators/`, covering all 44 operators since the ordering and rounding families are factory-built; 8 in `src/dev/` showcases; 61 across 15 test files). It is a required field, so every site lands in the same commit or `pnpm typecheck` is red.
_Spec: "`category` — the closed vocabulary" and "Introspection: `getOperators()`" (with "The snapshot shape") in [v3-operator-contract.md](v3-operator-contract.md); "Introspection & housekeeping methods" in [v3-evaluator-methods.md](v3-evaluator-methods.md). Naming recommendation for whoever builds it, so it isn't re-litigated: `OperatorInfo` / `FragmentInfo` / `Dependencies`, exported from the root, assembled in a new `src/introspect.ts`. The README generator's grouping by `category` is a follow-on, not this phase._
_Built (September 2026): one new module, [src/introspect.ts](../../src/introspect.ts), holding both snapshots and the dependency reshape, with the recommended names taken as given. **The chunk order inverted at the build**, which is the one sequencing finding: `getFragments()` reports its rollup in the public `Dependencies` shape (Carl, at planning — one reshape serves both methods, so a host compares like with like), and that shape is 13.2's, so the dependency machinery landed first and the snapshots on top of it. **The `category` check is collected rather than gated**: `defineOperator()`'s fast-fail gate exists for the fields without which nothing else is checkable, and a missing category blocks no other check — so a definition wrong in the category AND in a parameter reports both, which is the posture every other field already has. The sweep came to **111 sites, not the estimated 104**: the seven extra are raw `OperatorDefinition` literals in `test/fixtures/operatorDefinitions.ts` that never pass through `defineOperator()` at all, and they are also where the three new negative rows live (absent, non-string, outside the vocabulary). Two smaller readings: `FragmentParameter` had to join the barrel's public types, since it travels inside `FragmentInfo`; and the parameter snapshot is a spread of the validated declaration plus at most one key, so "fresh objects, frozen structures shared" costs one object per parameter per call and nothing below that._

**13.2 · `getDependencies()` / `isEvaluable()` / `version`, and the disposition checklist.** Three reads plus one compile-layer correction. `getDependencies()` is a reshape of `artifact.dependencies` — which Phase 3 records and Phase 11 already composes transitively through call sites — with traversal-order sorting of `paths`, discovery order for `operators` / `fragments`, and never-throws. **The compile-layer correction it forces**: the artifact records rendered _strings_ today, and `renderSegments` is lossy (`{ $get: { path: ['first.last'] } }` renders identically to `$data.first.last`, deduplicating two different reads into one entry and mis-splitting on any re-parse) — so recording moves to `PathSegment[]` deduplicated on a canonical key, and the renderer adopts the bracket-quoted key form the grammar already parses. `validate()`'s sample-data check is the existing consumer and gets the parsed form it already accepts. `isEvaluable()` is `artifact.holes.length > 0`; `version` is a readonly property. The v2→v3 method-disposition table becomes a checklist test (deleted methods absent, kept methods present) — instance methods only; the standalone structural export stays Phase 14's.
_Spec: "`getDependencies()`", "Introspection & housekeeping methods" and "v2 → v3 method disposition" in [v3-evaluator-methods.md](v3-evaluator-methods.md)._
_Built (September 2026). **The lossless renderer doubles as the deduplication key**, which is what keeps the correction to one idea rather than two: `renderSegments` bracket-quotes any key the dot grammar cannot carry (one holding `.`, `[`, `]`, a quote or a backslash, and the empty key), which makes it injective, so the canonical key a path is stored under is simply its render — there is no second notion of path identity to keep in step. `parsePath(renderSegments(s)) === s` is asserted as a property over the awkward keys. A sibling `renderDataReference` renders the `$data` reference form for messages, which fixes a cosmetic bug in the sample-data warning on the way through (`$data[0].x`, not `$data.[0]`). **Both new reads compile without the static-check pass**: `runStaticChecks` only appends issues, so holes and dependencies are entirely pass-1 products and the second walk would be work nobody reads — the cache exclusion the spec asks for is unaffected, and is asserted through the compile counter the compile-cache suite already uses. One consequence of traversal order worth recording, because it is counter-intuitive in exactly the case that motivated the correction: a single key holding a dot sorts **after** the two-level read it used to collide with (`first.last` then `["first.last"]`), because the comparison reaches `'first'` before `'first.last'` at segment 0. The disposition checklist is `test/surface.test.ts` alongside the `isEvaluable` rows and `version`. **Three corrections from the Phase-13 review**, all in the record rather than the surface: the segments are **canonicalized before they are keyed** (`canonicalSegments` in the primitives — a digit-only key becomes an index, since `resolvePath` reads `x.0` and `x[0]` as one read and a record of reads must too), so the injective render is canonical on reads and not merely on spellings; the render-keyed **map travels through the artifact** as `dataPaths` instead of being flattened to an array and rebuilt at every compose site, so a path renders exactly once, at record time, and `getDependencies()` sorts entries and reports keys; and a literal `get` path the recorder cannot enumerate — an empty one (the whole data object, like a bare `$data`), a non-array, an array holding a non-segment, or a string the grammar rejects — **flips `dynamic`** rather than vanishing from the record. `isEvaluable()` gained its static-error arm the same review (recorded in the Evaluator-methods spec)._

**13.3 · `compile()` and the `CompiledExpression` handle, with the result-generation bump.** One public method over the existing compile-with-checks seam, returning a holdable handle — `issues`, `hasErrors`, `expression`, `evaluate()`, `getDependencies()`, a stand-in `prettyPrint()` — with the design settled in [#155](https://github.com/CarlosNZ/fig-tree-evaluator/issues/155): a handle rather than the artifact, a snapshot of the whole instance state, the compile cache shared with `evaluate()`, an inert flavour behind the same type, the result store shared. [#174](https://github.com/CarlosNZ/fig-tree-evaluator/issues/174) rides along: a registry-affecting `updateOptions()` moves the result generation on, closing the instance's half of the redefinition hazard the handle's sharing widens. Brought forward from post-v3.0 once the design closed; purely additive.
_Spec: "`compile()`" and "Rulings on the surface" in [v3-evaluator-methods.md](v3-evaluator-methods.md)._
_Built (September 2026). The only new code is the class, beside `FigTree` in `src/FigTree.ts`: the tail of `evaluate()` — inert answer, static gate, run, unwrap — was lifted into one module-level function both methods call, and the handle holds the compile cache's own entry type, whose two shapes are the two flavours. Two readings at the build (Carl): an inert handle under `trace` compiles fresh as `evaluate()` does, and with the trace check inside the shared tail a traced `evaluate()` is served from the compile cache where it used to bypass it; and the inert flavour's lazy compile keeps the artifact, one node for a constant, so every getter reads one accessor. The lift surfaced one contract to keep by hand: `evaluate()` is no longer itself `async`, so the shared tail is, and a refused per-call option still rejects rather than throws. #174 is a three-line `invalidate()` on the result cache that `clearCache()` also calls; its arrival rewrote the one test that pinned the old "an `operatorDefaults` change touches no result" reading. `CompiledExpression` is a type-only root export; README and CHANGELOG are untouched, both still being v2-era. Tests: `test/compile-method.test.ts`, the handle rows in `test/evaluate-types.test.ts`, `'compile'` in the surface checklist, and the rewritten invalidation block in `test/result-cache.test.ts`._

_One duplication the sweep exposed and left standing: `codegen/buildOperatorReference.ts` carries a hand-maintained name→group table, whose group column is now a second source of truth beside `category` (and says `special` where the contract says `other`). Reconciling it belongs with the README/reference-generator grouping this phase already defers._

_Deliberately **not** here: the global cache bypass ([#166](https://github.com/CarlosNZ/fig-tree-evaluator/issues/166)) — an Options-area ruling that arose at this phase's planning and was separated because the snapshot shape does not depend on it. Editor seeds are not here either, and never will be: they left the runtime at the parameter passes and land in `./editor-hints` at Phase 14, which is why `default` in a snapshot means the runtime default and nothing else._

---

## Phase 14 — Packaging ⚠

Subpath exports, rollup build, `editor-hints` module, bundle-size checks, tree-shaking verification. **Spec: [v3-packaging.md](v3-packaging.md)** — drafted July 2026, awaiting review (7 open questions at its end).

---

## Phase 15 — Converter & differential (testing-strategy steps 4–5)

**15.1 · `convertV2ToV3`** in `./convert`, built on the compiler's normalizer.
**15.2 · The differential runner** over the frozen `V2/` corpus: `evaluate(convert(v2Tree))` vs recorded expected values; the **divergence catalog** (non-convertible / intentional-semantic-change / lossy-default) as a first-class output feeding the migration docs. Expect this phase to surface spec gaps — treat each as a spec-refinement loop, per the testing-strategy note.

---

## Phase 16 — Benchmarks: v2 vs v3, head to head (the last act for `/v2-src`)

**Split into 16a and 16b, and 16a runs before Phases 14–15** (assessed September 2026). Packaging is a size concern, not a speed one, and contributes nothing to either half. The converter binds **only 16b**: the synthetic cases are authored on both sides by construction, and the `massiveQuery` corpus comes from a generator this repo owns, so neither needs a mechanical conversion to be honest. Running 16a early is also where its value is — "v3 could plausibly lose on the small cold expression" is a finding worth having while there is still room to act on it, not after packaging is frozen. One results doc, written by 16a and extended by 16b.

**16a.0 · Prerequisites.** `/v2-src` is **not currently runnable**, so enabler #1 is already false: `dequal` left `package.json` when Phase 4 vendored its semantics into `equal`, and v2's `EQUAL` still imports `dequal/lite` — `pnpm test:v2` fails at import. Every other external import in `/v2-src` (`axios`, `node-fetch`, `object-property-extractor`, `pg`, `sqlite`) is still installed, so restoring the one devDependency is the whole fix. Do it regardless of when this phase runs: the frozen corpus is the converter's oracle as well as this phase's other arm, and it rots silently while nothing runs it. Benchmarks run on a Node satisfying `engines` (>=22) so numbers stay comparable across runs.

**16a.1 · Harness.** The methodology is already proven in [src/dev/compileCacheBench.ts](../../src/dev/compileCacheBench.ts) ([#161](https://github.com/CarlosNZ/fig-tree-evaluator/pull/161)) and is lifted rather than re-decided: `process.hrtime.bigint()`, the min of five rounds after a warm-up (the min is the least noise-contaminated estimator, since noise only ever adds), every result folded into a sink so nothing is optimized away. What that file lacks is a second arm — the addition is a v2 side running the same case against `/v2-src`, plus an equality assertion across the two, because a case whose arms disagree is not a measurement. Both sides run from TS source: symmetric, so the relative claims hold, but absolute cold figures move once Phase 14 settles packaging — **re-run the doc after 14** before it becomes the release-notes story. **Numbers are only comparable on one runtime, and it is Node 22** (`.nvmrc`; `codegen/bench.mjs` refuses below it). The first run of the suite was taken on Node 20.20 by accident and overstated v3's per-node cost by about half: Node 20's `AbortController` lifecycle is ~2.5 µs against Node 22's ~0.34 µs, and v3 pays one per lazily-delivering node. On 22 a v3 operator node costs ~2.3–3.1 µs against v2's ~1.1–1.3; on 20 it read ~5. **The browser agrees with Node 22, not Node 20.** The front end is where most evaluation happens, so the benches also bundle for a browser (`pnpm bench:browser <name>…` → `bench/browser/dist`, served by `pnpm bench:serve`; ES modules over HTTP with cross-origin isolation, which lifts Chrome's `performance.now()` clamp from 100 µs to 5 µs) and were run in Chromium 153 through Playwright. Chrome's `AbortController` is native where Node's is JavaScript, and it prices about like Node 22's: every ratio matches Node 22 within noise — `holes` 1.2× at one section and 144× at a thousand, `parseOnce` 0.7–0.9× operator-dense and 10.6× on reads, the Conforma form 0.7× / 0.8× / 0.9× across the three granularities — with both engines ~25–30% faster in absolute terms (a full form re-evaluation is 1.03 ms in v3 content against 0.68 ms in v2). A v3 operator node costs ~2.3 µs there against v2's ~0.8. Safari and Firefox are different engines and remain unmeasured; Playwright can drive both once installed.

**16a.1b · The four standard arms** (Carl, September 2026). Every bench declares the same four, so a claim is never read in one cache state and quoted in another: **v2**; **v3 identity**, the held expression object on the compile cache's identity layer; **v3 content**, identity missed and the content layer hit; and **v3 cold**, no compile cache at all. Two of the three v3 arms need a mechanism, and both are chosen so the timing loop is not charged for building the arm. _Content_ passes `{ ...tree }`, a shallow spread: a new top-level object defeats the `WeakMap` while the serialized bytes underneath are unchanged, and it costs **9.7 ns** whatever the config's size, because only the top-level keys are copied. _Cold_ constructs a fresh `FigTree` per evaluation, because both layers live on the instance and there is deliberately no clearing API ("invalidation is replacing the whole cache" — src/compile/compileCache.ts); the alternative, building a fresh expression each iteration, would put an O(input) allocation inside the loop. A fresh instance costs one registry build instead — constant, independent of the config, and printed by each bench so the cold column can be read net of it. The consequence for the ledger below: **claim 4, the floor, is no longer its own case** — it is the `v2 / v3 cold` column of every bench.

**16a.2 · The claims ledger.** "Each claimed win" is enumerated here rather than gestured at, so that a case exists per row and no claim ships unmeasured. I/O is excluded or zero-latency mocked throughout (network variance would swamp the signal).

| Claim                                                                                                                                   | Where it is made                                                                                                                          | The case                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **O(holes) per evaluation** — a config compiles to a constant skeleton plus a list of holes, and each evaluation touches only the holes | "Deep evaluation" in [v3-api.md](v3-api.md)                                                                                               | Whole config, few holes, evaluated repeatedly, against v2 `evaluateFullObject`'s full re-walk. The headline claim, and the one two later rulings were made to protect — the `structuredClone` rejection in [v3-evaluator-methods.md](v3-evaluator-methods.md), and top-level-only boundary normalization in [v3-operator-contract.md](v3-operator-contract.md)                                                                                                                                                                                                                                            |
| **Compile-once, O(1) thereafter** — recognition, alias normalization and positional mapping happen at compile and never again           | "Ruling: no `v2Compat` runtime flag" in [v3-migration.md](v3-migration.md); the compile-once note in [v3-assessment.md](v3-assessment.md) | One held expression, changing data, many evaluations, against v2's per-call recognition and alias walk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Instance churn costs one serialize+hash, then O(1)**                                                                                  | "Cache keying for non-identical inputs" in [v3-implementation-notes.md](v3-implementation-notes.md)                                       | Fresh instance per call, identical expression bytes. **Partly measured already** by #161, but against v3's own recompile: 6–8× for expression-dense input, 1.2–1.6× mostly-static, and an inversion to 5.5× (30× at 200 kB) for a large opaque payload behind a single hole. What is missing is where those three sit against **v2**, which is the comparison the claim actually makes                                                                                                                                                                                                                    |
| **Floor: all layers miss is still roughly v2's baseline**                                                                               | same entry                                                                                                                                | Not a case of its own: the `v2 / v3 cold` column of every bench. **Measured September 2026, on Node 22** (the first run, on Node 20, read twice as badly for v3 — see 16a.1): v3 cold loses only on a one-section config (0.6×) and is level with v2 from ten sections up, so the floor holds everywhere but the very bottom, cold. v3 _identity_ beats v2 even there (9.4 µs against 11.1). The Node 20 reading had v3 losing in every cache state below fifty sections — a real effect of that runtime's `AbortController`, not of the engine, and the reason `pnpm bench` refuses to run below Node 22 |
| **The constancy probe skips the compile for inert values**                                                                              | "Skip the compile for inert inputs" in [v3-implementation-notes.md](v3-implementation-notes.md)                                           | Measured in September 2026 against v3's own compiler (3.6–10.6× cheaper on inert values; +40% worst case on a config whose holes sit late) but **never against v2**, which returned such values untouched after a type check. The spec pins the property here explicitly: "a performance property, pinned at Phase 16, not by a unit test"                                                                                                                                                                                                                                                                |
| **Memoized I/O steady state** — O(1) compile-cache lookup + O(holes) work + memoized I/O                                                | the fetch-count walkthrough in [v3-worked-examples.md](v3-worked-examples.md)                                                             | Zero-latency mocked client, repeated evaluation: assert the call count and measure per-call cost against v2's cache                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Boolean short-circuit and cancellation** — a decided `and`/`or` resolves without its remaining operands                               | "Deterministic error semantics" in [v3-operator-parameters.md](v3-operator-parameters.md)                                                 | v2's `AND`/`OR` evaluate **every** operand eagerly through `evaluateArray` before reducing, so this is a real difference rather than parity — measured with costly synchronous operands, since mocked I/O hides it                                                                                                                                                                                                                                                                                                                                                                                        |

Two scale cases carry no single claim: **deep nesting**, and a **large generated tree**. _The second was planned as the 20,000-node `massiveQuery` corpus, on the grounds that its generator ([codegen/queryBuilder.ts](../../codegen/queryBuilder.ts)) is ours and could grow a v3 arm. Corrected at the build (September 2026): it cannot, not in 16a. Its `baseExpressions` are close to v3 spelling only for the first two thirds; the rest carry `outputType`, `type`, `funcName`, `passThru` and positional `children`, which is conversion work rather than a change of spelling, and hand-translating them would produce a v3 tree nobody had checked against the recorded v2 result — the dishonesty the 16a/16b split was drawn to avoid. `massiveQuery` therefore moves to **16b**, and 16a builds its own large tree from one shared shape in both spellings, like every other case here._

**The real-world corpus** (Carl, September 2026): a production Conforma template — 62 form elements and 19 actions — as two benches, `conformaElements` and `conformaActions`, over `bench/real-world/`. The v2 export is kept byte-identical and the v3 spelling is produced by `bench/real-world/migrate.ts`, a corpus-specific mapping (fifteen operators, three fragments, three host functions) that throws on any shape it was not taught; `fixtures.ts` supplies data under which every one of the corpus's 80-odd paths resolves, instant HTTP and SQL stand-ins, and the host functions registered both ways. Every unit is checked equal across all arms before timing (`real-world/diff.ts` prints every disagreement, for when the migration changes). Elements run the granularity matrix Carl asked for — every leaf on its own, `parameters` whole, element whole — on the four arms, with content the realistic one; actions run cold only, being once-per-event. _Three migration rules were semantic, each verified on both engines first: v2's `join` is a PLUS alias; `strict: false` made `<`/`>` inclusive; and v2's `{{token}}` resolved unmatched tokens as data paths — a path into the substitutions object when that was itself a node — where v3 named tokens are keys only, so each such token becomes `{{pN}}` with an explicit `get`. Conforma's `.list.key` drills become `[*]` projections, which is what v2's map-over-array meant._

_Not a claim — checked and recorded so that nobody builds a case for it: **branch laziness is parity**. v2's `CONDITIONAL` and `MATCH` already evaluated only the taken branch, by hand, in their own bodies. What v3 wins there is that laziness is a declared parameter property instead of per-operator handwork — an architecture result, not a measurable one._

**16b.1 · The frozen-corpus arm** (needs Phase 15). The **largest expression trees from the frozen V2 suite**, run as `evaluate(convert(v2Tree))` on v3 against the same tree on `/v2-src` — the converter is what makes "same logic both sides" true rather than asserted. This leg is deferred for a reason beyond its dependency: the biggest trees in the corpus, the "mother of all expressions" in `test/V2/17_complexExpressions.test.ts` among them, are dense with the I/O this phase mocks away, so they exercise conversion fidelity at scale more than they exercise speed. 16a carries the performance story; 16b is what confirms it survives contact with real v2 config.

Output: a recorded results doc — the release notes' performance story; a non-blocking CI job at most. Any case where v3 fails to beat v2 at its own claimed game is a pre-release finding to investigate, not a footnote. **`/v2-src` is deleted only after 16b.**

---

## Milestones

| #   | After     | You can…                                                                                     |
| --- | --------- | -------------------------------------------------------------------------------------------- |
| M1  | Phase 3   | `validate()` any expression — the editor's static half works with zero evaluation capability |
| M2  | Phase 4   | evaluate eager expressions end to end, throw mode                                            |
| M3  | Phase 7   | run all 41 core operators — bulk of migrated v2 tests green                                  |
| M4  | Phase 9   | full I/O with caching — the lifecycle example passes with observable fetch counts            |
| M5  | Phase 13  | feature-complete engine, whole method surface                                                |
| M6  | Phase 16a | the claims ledger measured against v2 — the performance story, ahead of packaging            |
| M7  | Phase 15  | converter + differential green — migration-ready                                             |
| M8  | Phase 16b | the story confirmed over real v2 config — and `/v2-src` retired                              |

## Bundle size by phase

Bundle size is a v3 goal in its own right ([v3-packaging.md](v3-packaging.md)), so it is tracked as the engine is built rather than discovered at Phase 14. `pnpm build` prints the numbers below plus a per-module table; `pnpm size` re-prints them without rebuilding. **Recorded at the close of each phase** (working rule 6), measured on the phase's last commit.

Per-PR movement is caught without anyone remembering to look: `.github/workflows/pr-bundle-size.yml` builds both sides of a pull request and posts the difference as a sticky comment, so growth is attributable to the change that caused it rather than noticed a phase later. It shares the measuring code with the report above. Phase 14's _size budget_ (packaging, Build & CI mechanics) is a separate check still to come — a threshold assertion, once there is a number worth asserting.

`minified` is the published `build/index.js` — ESM, terser, tree-shaken from `src/index.ts`; there are no external runtime dependencies (`dequal` is vendored as `deepEqual` from Phase 4). `brotli` is the figure that matters for a browser consumer; `types` is the rolled-up `index.d.ts`, uncompressed.

**What the figure is, and is not.** It is the whole public surface reachable from the main entry point — a ceiling, not a per-consumer cost. Because the package is ESM with `sideEffects: false`, a consumer's own bundler shakes the single published file down to what they actually import. Measured against the Phase-3 build by rolling up a consumer entry per import subset:

| A consumer importing…                      | raw      | brotli   | _(Phase 3, for contrast)_ |
| ------------------------------------------ | -------- | -------- | ------------------------- |
| `version` only                             | 0.04 kB  | 0.05 kB  | 0.21 kB                   |
| two primitives (`isTruthy`, `resolvePath`) | 1.75 kB  | 0.77 kB  | 1.96 kB                   |
| `FigTree`                                  | 82.52 kB | 22.31 kB | 30.38 kB                  |
| `FigTree` + `defineOperator`               | 82.54 kB | 22.31 kB | 43.41 kB                  |
| everything (`import * as`)                 | 85.62 kB | 22.83 kB | 44.57 kB                  |

_Re-measured at Phase 7 on the same method (a consumer entry per import subset, rolled up from `src/index.ts` with `moduleSideEffects: false`, which is what the package's `sideEffects: false` tells a consumer's bundler). The two ends of the range still hold — a consumer who imports a primitive still pays for a primitive — but the middle has collapsed into one figure, which is the finding below._

One caveat the figure cannot carry: the HTTP/SQL clients are deliberately consumer-supplied ([v3-packaging.md](v3-packaging.md)), so anyone using `GET`/`SQL` pays for `axios`/`pg` on top — both dwarf the engine.

| After                                                  | minified  | gzip     | brotli   | types    | Largest contributors (pre-minify share)                                                          |
| ------------------------------------------------------ | --------- | -------- | -------- | -------- | ------------------------------------------------------------------------------------------------ |
| Phase 0 — skeleton                                     | 0.04 kB   | 0.06 kB  | 0.04 kB  | 0.66 kB  | `version.ts` only                                                                                |
| Phase 1 — foundations                                  | 5.70 kB   | 2.31 kB  | 2.11 kB  | 4.42 kB  | `path` 34%, `typeCheck` 33%, `FigTreeError` 15%                                                  |
| Phase 2 — definitions & registry                       | 22.48 kB  | 6.99 kB  | 6.29 kB  | 9.10 kB  | `defineOperator` 50%, `registry` 14%, `typeCheck` 14%                                            |
| Phase 3 — compiler + `validate()`                      | 44.17 kB  | 13.43 kB | 12.10 kB | 10.43 kB | `compile` 30%, `defineOperator` 25%, `staticChecks` 12%                                          |
| Phase 4 — evaluator core + 24 eager operators          | 62.69 kB  | 19.07 kB | 17.07 kB | 14.56 kB | `compile` 21%, `defineOperator` 18%, `staticChecks` 9%, `math` 6%, `params` 6%                   |
| Phase 5 — scoping & laziness + 6 logic operators       | 71.05 kB  | 21.39 kB | 19.16 kB | 14.57 kB | `compile` 21%, `defineOperator` 16%, `staticChecks` 8%, `params` 7%, `math` 5%                   |
| Phase 6 — iterators + 5 operators                      | 75.59 kB  | 22.66 kB | 20.30 kB | 14.64 kB | `compile` 20%, `defineOperator` 15%, `params` 8%, `staticChecks` 8%, `typeCheck` 5%              |
| Phase 7 — remaining core operators (core-complete)     | 85.64 kB  | 25.66 kB | 22.86 kB | 14.87 kB | `compile` 21%, `defineOperator` 13%, `staticChecks` 7%, `params` 6%, `string` 6%                 |
| Phase 8 — the instance layer (options + compile cache) | 87.60 kB  | 26.28 kB | 23.36 kB | 14.91 kB | `compile` 21%, `defineOperator` 13%, `staticChecks` 7%, `params` 6%, `string` 6%                 |
| Phase 9 — result cache + I/O (3 operators, 4 clients)  | 101.84 kB | 30.84 kB | 27.34 kB | 17.43 kB | `compile` 18%, `defineOperator` 11%, `staticChecks` 6%, `params` 5%, `string` 5%, `io` 4%        |
| Phase 10 — kill switch & shielding                     | 103.04 kB | 31.26 kB | 27.73 kB | 17.43 kB | `compile` 18%, `defineOperator` 11%, `staticChecks` 6%, `params` 5%, `string` 5%, `typeCheck` 4% |
| Phase 11 — fragments                                   | 112.05 kB | 33.56 kB | 29.68 kB | 17.88 kB | `compile` 17%, `defineOperator` 10%, `staticChecks` 6%, `params` 5%, `string` 5%, `fragments` 5% |
| Phase 12 — report mode & trace                         | 117.14 kB | 35.25 kB | 31.23 kB | 19.72 kB | `compile` 16%, `defineOperator` 10%, `staticChecks` 6%, `params` 5%, `string` 5%, `fragments` 4% |
| Phase 13 — introspection (surface complete)            | 120.32 kB | 36.15 kB | 31.94 kB | 21.23 kB | `compile` 16%, `defineOperator` 10%, `staticChecks` 5%, `string` 5%, `params` 4%, `fragments` 4% |

Phases 0–2 were measured retroactively by building each phase's `src/` with the current toolchain, so the columns are apples to apples (the Phase-3 row reproduces the live build exactly). The package has no runtime dependency to understate: `dequal` was vendored into the bundle with Phase 4's `equal`. The Phase-0 gzip figure exceeding its minified figure is just container overhead on a 40-byte file.

Two things to watch, not yet act on. **`defineOperator` has stopped being optional, and it happened earlier than this plan predicted.** The Phase-3 note said it would stop shaking off "the moment `coreOperators` is itself built with it, which is Phase 7" — but `coreOperators` has been built with it since **Phase 4**, and the measurement confirms the gap between the `FigTree` and `FigTree + defineOperator` rows was already 0.02 kB at Phase 6, where at Phase 3 it was 13 kB. Importing the class now brings the whole registration validator, because the core definitions run through it at module scope. And `compile` + `staticChecks` is 28% of the bundle (down from 42% in share, up in absolute terms): authoring-time machinery that a consumer who only evaluates still pays for, since it is reachable from the class. Whether either splits out is a **Phase 14** packaging decision — the numbers accumulated here are its evidence, and the first of the two now has a measured cost rather than a predicted one.

## Standing dependencies & flags

- **Spec gates**: discharged July 2026 — evaluator-methods signed off (Qs 1–3 + 11 settled at close-off; Q12's operatorDefaults-required ban signed off and amended into Options); Packaging drafted in [v3-packaging.md](v3-packaging.md), awaiting review before Phase 14.
- **`/v2-src` runnability**: the frozen engine is load-bearing twice over — Phase 15's converter oracle and Phase 16's other arm — and it is **currently broken** (`dequal` left `package.json` at Phase 4; v2's `EQUAL` still imports `dequal/lite`). Restored as a devDependency September 2026, and `pnpm test:v2` is green again at **516 of 517** — the one failure is `25_metaData`, where Jest 30's `toStrictEqual` no longer lets `expect.objectContaining` match a function, so the corpus records a v2 behaviour the runner can no longer express. That is a runner artifact, not a v2 regression, and the record is never edited to accommodate it. Keep the suite on tap until 16b deletes the folder; nothing in CI runs it, so it rots unwatched. _A second interop hazard lives outside the folder and is worked around in `tsconfig.bench.json`: `object-property-extractor` ships an ESM build that declares no `"type": "module"`, which only bites an ESM importer — jest never sees it._
- **Environment**: 9.3's live-network and Northwind-SQL tests are tagged, never blocking CI; the mock client is the primary oracle.
- **Contract watch-list**: the chunks most likely to bounce details back to [v3-operator-contract.md](v3-operator-contract.md) are 5.2/5.3 (mode vocabulary, Q1), 9.1 (caching split, Q2), 4.1 (absent-key delivery, Q4) — budget review time there.
