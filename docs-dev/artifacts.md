# Artifacts

Supplementary documents published as Claude artifacts — pages that live on claude.ai rather than in the repo, because they're presentation rather than specification. This file is the repo's record of what exists and where.

Each entry carries two links. The **artifact** link is the page as published on claude.ai, and it resolves for its owner and for members of the organization only: public links and email invites to people outside the organization are both turned off by the org's admin, so a `?sk=` share key does not make one readable by anyone else. The **rendered** link serves the committed copy in [../docs-artifacts/](../docs-artifacts/) through githack, which re-serves a raw GitHub file as `text/html` where `raw.githubusercontent.com` deliberately sends `text/plain` with `nosniff` — so a rendered link is public, because the repository is, and needs no GitHub Pages site.

Rendered links point at `main` and resolve once the v3 work merges there; until then, substitute the branch that holds the files (`V3-phase7-remaining-core`) for `main` in the URL. For a link that must never move, pin a commit SHA in place of the branch and use `rawcdn.githack.com`, which is githack's permanently-cached host — `raw.githack.com` is rate-limited and meant for development.

- [FigTree v3 by Example](https://claude.ai/artifact/69z5N3h1r3UKbqQQZesAQC?sk=FL4gCRl5_1IMkNy4k3bqSQ) — nine walk-through v3 expressions, each a step harder than the last, from a single `$data` reference to a whole dashboard config with report mode and timeout shielding. Companion to the specs in [v3-specs/](v3-specs/). Rendered: [figtree-v3-by-example.html](https://raw.githack.com/CarlosNZ/fig-tree-evaluator/main/docs-artifacts/figtree-v3-by-example.html) _2026-09-17_
- [FigTree From First Principles](https://claude.ai/artifact/Mu8ESY7vqhtZVkQvDQdXnG?sk=Wujwr3OF8ZwZUYaIl9pwqQ) — the whole v3 model derived forwards in fourteen stages, each mechanism justified by the problem that forces it, in plain language with the spec vocabulary introduced as it arrives. The on-ramp the fifteen spec docs don't provide. Rendered: [figtree-from-first-principles.html](https://raw.githack.com/CarlosNZ/fig-tree-evaluator/main/docs-artifacts/figtree-from-first-principles.html) _2026-09-17_
- [FigTree v3 Operators](https://claude.ai/artifact/KEw7w1N3gvLGXpSaUjMGwy) — every operator as a header over its parameters: the shorthand call shape, each parameter with its type, default and delivery mode, and what the node returns — filterable by name, alias, parameter or group. Built from the repo by `pnpm generate:operators`, which reads the registered definitions themselves, so it is **generated locally** into [docs-artifacts/](../docs-artifacts/) rather than downloaded, and republished from that file after each phase that lands operators. Supersedes the operator table formerly at `v3-specs/v3-operator-signatures.md`. Rendered: [figtree-v3-operators.html](https://raw.githack.com/CarlosNZ/fig-tree-evaluator/main/docs-artifacts/figtree-v3-operators.html) _2026-09-19 — republished at the close of Phase 7: 40 operators now read from their own definitions, 4 still spec-sourced (`literal` permanently, being grammar rather than a definition, plus the three I/O operators)_
- [FigTree Editor Hints](https://claude.ai/artifact/Ap3aWoLa183BH5QN4Bp4NE) — every core and I/O operator as it will display in an editor: display name, colours with their contrast ratio, starting-value seeds, and its v2 counterpart, grouped under the eight category colours, plus the per-type seeds. Generated from `src/editor-hints/index.ts` for the Phase 14.2 review. _2026-09-24_
