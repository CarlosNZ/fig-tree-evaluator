import { useState } from 'react'
interface EditorProps {
  type: 'data' | 'figTree'
  localStorageKey: string
}

export const Editor: React.FC<EditorProps> = ({ type, localStorageKey }) => {
  return <></>
}
